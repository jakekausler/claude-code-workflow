{
  "name": "mcp-server",
  "version": "0.1.0",
  "description": "MCP server for kanban workflow tools",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "kanban-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "verify": "npm run lint && npm run test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "kanban-cli": "file:../kanban-cli",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
